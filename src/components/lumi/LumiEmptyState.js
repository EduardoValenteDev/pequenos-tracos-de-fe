/**
 * LumiEmptyState — wrapper de compatibilidade.
 * Delega para BeniEmptyState mantendo a mesma assinatura de props.
 * Não remova este arquivo — telas antigas importam daqui.
 */
import BeniEmptyState from '../beni/BeniEmptyState';

export default BeniEmptyState;
