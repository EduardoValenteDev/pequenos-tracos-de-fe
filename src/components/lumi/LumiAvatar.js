/**
 * LumiAvatar — wrapper de compatibilidade.
 * Delega para BeniAvatar mantendo a mesma assinatura de props.
 * Não remova este arquivo — telas antigas importam daqui.
 */
import BeniAvatar from '../beni/BeniAvatar';

export default BeniAvatar;
