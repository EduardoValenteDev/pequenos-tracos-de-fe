/**
 * LumiLockedState — wrapper de compatibilidade.
 * Delega para BeniLockedState mantendo a mesma assinatura de props.
 * Não remova este arquivo — telas antigas importam daqui.
 */
import BeniLockedState from '../beni/BeniLockedState';

export default BeniLockedState;
