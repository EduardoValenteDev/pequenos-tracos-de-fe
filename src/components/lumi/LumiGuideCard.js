/**
 * LumiGuideCard — wrapper de compatibilidade.
 * Delega para BeniGuideCard mantendo a mesma assinatura de props.
 * Não remova este arquivo — telas antigas importam daqui.
 */
import BeniGuideCard from '../beni/BeniGuideCard';

export default BeniGuideCard;
